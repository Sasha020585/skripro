using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Destory : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //уничтожует обьект в начале игры
        Destroy(gameObject);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
